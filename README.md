really moving react component library

## Dev
'run' - npm install

This library project uses storybook, 'npm run storybook'

## Build

To build run 'npm run rollup-build'

## Publish

To publish run 'npm publish' - requires access token