import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import Breadcrumb from './Breadcrumb'
import HeaderSimple from '../HeaderSimple'

<Meta 
    title="Components/Breadcrumb" 
    component={Breadcrumb}
    argTypes={{
    Breadcrumb: {
        description: 'Text to go in Breadcrumb'
    }
  }}
/>

export const Template = args => (
    <Breadcrumb {...args}>
        <Breadcrumb.Crumb>Home</Breadcrumb.Crumb>
        <Breadcrumb.Crumb>Conveyancing</Breadcrumb.Crumb>
        <Breadcrumb.Crumb>Partners</Breadcrumb.Crumb>
    </Breadcrumb>
)

<HeaderSimple title='Breadcrumb' />

## Overview

Breadcrumbs help users navigate the site.

<Canvas>
    <Story
    name = "Overview"
    args = {{
        Breadcrumb: 'Home, removals'
    }}>
        { Template.bind({}) }
    </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />