import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import Logo from './Logo'
import HeaderSimple from '../HeaderSimple'

<Meta 
    title="Components/Logo" 
    component={Logo}
    argTypes={{
    type: {
        description: 'The type of logo used',
        width: 'set the logo width',
        options: ['horizontal', 'stacked', 'twenty', 'footer'],
      control: { type: 'radio' },
    }
  }}
/>

export const Template = args => <Logo {...args} />

<HeaderSimple title='Logo' />

## Overview

Company logos to be used throughout the site.

<Canvas>
    <Story
    name = "Overview"
    args = {{
        type: 'horizontal'
    }}>
        { Template.bind({}) }
    </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />