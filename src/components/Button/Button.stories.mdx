import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Button from './Button'

<Meta title="Components/Button" component={Button} />

export const Template = args => <Button {...args} />

# Button

Sizes
<Canvas>
    {/* bit of hack to get it to display how I want */}
    <Story
    name = "Sizes"
    args = {{
        label: 'Large',
        type: 'primary',
        lg: 'lg'
    }}>
        { Template.bind({}) }
    </Story>
    <Story
    name = "_hidden_Small"
    args = {{
        label: 'Small',
        type: 'primary',
        sm: 'sm'
    }}>
        { Template.bind({}) }
    </Story>
    <Story
    name = "_hidden_X Small"
    args = {{
        label: 'X Small',
        type: 'primary',
        xs: 'xs'
    }}>
        { Template.bind({}) }
    </Story>
</Canvas>

Kinds
<Canvas>
    <Story
    name = "Kinds"
    args = {{
        label: 'Primary',
        type: 'primary'
    }}>
        { Template.bind({}) }
    </Story>
    <Story
    name = "_hidden_Secondary"
    args = {{
        label: 'Secondary',
        type: 'secondary'
    }}>
        { Template.bind({}) }
    </Story>
    <Story
    name = "_hidden_Transparent"
    args = {{
        label: 'Transparent',
        type: 'trans'
    }}>
        { Template.bind({}) }
    </Story>
    <Story
    name = "_hidden_Account"
    args = {{
        label: 'Account',
        type: 'account'
    }}>
        { Template.bind({}) }
    </Story>
</Canvas>