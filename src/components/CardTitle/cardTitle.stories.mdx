import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import CardTitle from './CardTitle'
import HeaderSimple from '../HeaderSimple'

<Meta 
    title="Components/Card/CardTitle" 
    component={CardTitle}
    argTypes={{
    CardTitle: {
        description: 'Text to go in CardTitle'
    }
  }}
/>

export const Template = args => <CardTitle {...args} />

<HeaderSimple title='CardTitle' />

## Overview

CardTitles can be used to label items such as cards to help users find what they are looking for and also improve SEO.

<Canvas>
    <Story
    name = "Overview"
    args = {{
        CardTitle: 'Home, removals'
    }}>
        { Template.bind({}) }
    </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />